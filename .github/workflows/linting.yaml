---
name: Linting
on:
  push:
    branches-ignore:
      - main
concurrency:
  group: ${{ github.ref }}
  cancel-in-progress: true
permissions:
  contents: read
jobs:
  linting:
    uses: daemon-labs-resources/.github/.github/workflows/linting.yaml@main
    permissions:
      contents: read
      packages: read
      statuses: write
      checks: write
